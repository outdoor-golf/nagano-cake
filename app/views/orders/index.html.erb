<h1>注文履歴一覧</h1>

<table>
  <tr>
    <th>注文日</th>
    <th>配送先</th>
    <th>注文商品</th>
    <th>支払い金額</th>
    <th>ステータス</th>
    <th>注文詳細</th>
  </tr>
  
  <tr>
    <%= @orders.each do |order| %>
    <td><%= order.created_at.to_s(:datatime_jp) %></td>
    <td><%= order.postal_code %></td>
    <td><%= order.order_details.each do |order_detail| %>
    <%= order_detail.food.name %>
    <% end %>
    </td>
    <td><%= order.total_price.to_s(:delimited) %></td>
    <td></td>
    <td></td>
  </tr>
</table>